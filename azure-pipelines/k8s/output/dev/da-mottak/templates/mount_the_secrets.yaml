---
# Source: da-mottak/templates/mount_the_secrets.yaml
kind: Pod
apiVersion: v1
metadata:
  name: da-mottak-dev-csi-attachment-mount-secrets
  namespace: da-mottak-dev
  labels:
    aadpodidbinding: "da-mottak-dev-id-keyvault"
spec:
  containers:
  - image: nginx
    name: nginx
    volumeMounts:
    - name: secrets-store-inline
      mountPath: "/mnt/secrets-store"
      readOnly: true
  volumes:
    - name: secrets-store-inline
      csi:
        driver: secrets-store.csi.k8s.io
        readOnly: true
        volumeAttributes:
          secretProviderClass: "da-mottak-dev-csidriver-keyvault"
