---
# Source: da-mottak/templates/oauth2-proxy.yaml
apiVersion: v1
kind: Service
metadata:
  name:  da-mottak-dev-oauth2-proxy-service
  namespace: da-mottak-dev
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 4180
      targetPort: 4180
  selector:
    run: oauth2-proxy
---
# Source: da-mottak/templates/oauth2-proxy.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: da-mottak-dev-oauth2-proxy-deployment
  namespace: da-mottak-dev
  labels:
    app: oauth2-proxy-pod
spec:
  replicas: 1
  selector:
    matchLabels:
      run: oauth2-proxy
  template:
    metadata:
      labels:
        run: oauth2-proxy
    spec:
      containers:
        - image: quay.io/oauth2-proxy/oauth2-proxy:latest
          imagePullPolicy: IfNotPresent
          name: nginx
          ports:
            - containerPort: 4180
          args:
            - "--provider=azure"
            - "--email-domain=arkivverket.no"
            - "--http-address=0.0.0.0:4180"
            - "--redirect-url=https://mottak.plattform.arkivverket.dev/oauth2/callback"
          env:
            - name: OAUTH2_PROXY_COOKIE_SECRET
              valueFrom:
                secretKeyRef:
                  name: oauth-cookie-secret
                  key: oauth-cookie-secret
            - name: OAUTH2_PROXY_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: oauth-client-id
                  key: oauth-client-id
            - name: OAUTH2_PROXY_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: oauth-client-secret
                  key: oauth-client-secret
