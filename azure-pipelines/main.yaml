# Explicitly set none for repositry trigger
trigger:
  - none

resources:
  containers:
    - container: test
      type: ACR
      azureSubscription: kriwal-test1
      resourceGroup: meta
      registry: arkivverket
      repository: av-mottak/kriwal-test
      trigger: true

pool:
  vmImage: 'ubuntu-latest'


steps:
  - script: |
      echo Hello world
    displayName: 'Command Line Script'

  - task: HelmDeploy@0
    displayName: Helm template
    inputs:
      command: template
      arguments: --values "values-dev.yaml" --output-dir k8s/output/dev k8s/
