# THIS FILE IS AUTOGENERATED. EDIT TEMPLATES IN PARENT FOLDER
on:
  push:
    branches: [ develop ]
    paths:
      - 'mottak-arkiv-web/**'
  pull_request:
    paths:
      - 'mottak-arkiv-web/**'

jobs:
  test:

    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: 'mottak-arkiv-web'
    strategy:
      matrix:
        node-version: [ 14.x ]

    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}

      - name: Installing app dependencies
        run: |
          yarn install

      - name: Run ESlint
        run: |
          yarn eslint .

      - name: Test with yarn
        run: |
          yarn
          yarn test

  build-and-push-docker-image:

    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: 'mottak-arkiv-web'
    needs: test
    if: github.ref == 'refs/heads/develop'

    steps:
      - uses: actions/checkout@v2

      - name: Build and push docker image
        uses: azure/docker-login@v1
        with:
          login-server: arkivverket.azurecr.io
          username: ${{ secrets.ARKIVVERKET_AZURE_REGISTRY_USERNAME }}
          password: ${{ secrets.ARKIVVERKET_AZURE_REGISTRY_PASSWORD }}

      - run: |
          docker build . -t arkivverket.azurecr.io/av-mottak/mottak-arkiv-web:${{ github.sha }}
          docker push arkivverket.azurecr.io/av-mottak/mottak-arkiv-web:${{ github.sha }}

